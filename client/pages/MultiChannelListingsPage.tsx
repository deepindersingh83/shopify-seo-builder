import { Layout } from "@/components/Layout";
import MultiChannelListings from "@/components/MultiChannelListings";

export default function MultiChannelListingsPage() {
  return (
    <Layout>
      <MultiChannelListings />
    </Layout>
  );
}
